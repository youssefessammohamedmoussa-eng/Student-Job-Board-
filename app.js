if (typeof I18N !== "undefined") {
  Object.assign(I18N.en, {
    userDashTitle: "My Applications",
    userDashDesc: "Track all jobs you applied for in one place.",
    companyDashTitle: "Company Dashboard",
    companyDashDesc: "Post jobs and review applicants (demo stored in localStorage).",
    jobTitle: "Title",
    publish: "Publish",
    companyNote: "(Demo) Posted jobs are saved in localStorage.",
    emptyTitle: "No results",
    emptyDesc: "Try changing filters or search keywords.",
    cvNote: "(Demo) Applications are stored locally in this browser.",
    cvBuilderCta: "CV Builder",
    cvBuilderCtaTitle: "Create a job-ready CV in minutes",
    cvBuilderCtaDesc: "Save your resume inside SJB and reuse it when you apply for student jobs.",
    openCvBuilder: "Open CV Builder",
    cvBuilderTitle: "CV Builder",
    cvBuilderDesc: "Build a polished CV, save it locally in SJB, and download it as a PDF when you are ready.",
    cvBuilderFormTitle: "Build Your Details",
    cvBuilderPreviewTitle: "Live CV Preview",
    cvPreviewHint: "Use Download PDF to save a multi-page CV file directly from this builder.",
    cvTargetRole: "Target Role",
    cvSummary: "Professional Summary",
    cvSkills: "Skills",
    cvEducation: "Education",
    cvExperience: "Experience",
    cvProjects: "Projects",
    cvLanguages: "Languages",
    cvWebsite: "Website / Portfolio",
    cvLinkedIn: "LinkedIn",
    cvSaveDraft: "Save Draft",
    cvReset: "Reset CV",
    cvExportPdf: "Download PDF",
    cvAddEducation: "Add Education",
    cvAddExperience: "Add Experience",
    cvAddProject: "Add Project",
    cvNoCvYet: "No CV saved yet",
    cvNoCvDesc: "Start your SJB CV now so you can reuse it when applying.",
    cvSaved: "Saved CV",
    cvLastUpdated: "Last updated",
    useSavedCv: "Use my saved SJB CV",
    useSavedCvDesc: "We'll attach the CV you built inside SJB to this application.",
    buildCvInstead: "Build a CV first",
    cvSourceBuilder: "SJB CV Builder",
    cvApplyRequired: "Please fill all fields and upload a CV or use your saved SJB CV.",
    cvDraftSaved: "Draft saved successfully.",
    cvDraftReset: "Draft reset.",
    cvResetConfirm: "Reset your saved CV draft?",
    companyBilling: "Billing",
    companyBillingTitle: "Billing & Plan",
    companyBillingDesc: "Choose a plan, save a demo payment method, and keep your company account ready to hire.",
    companyBillingManage: "Manage Billing",
    companyBillingOpen: "Open Billing",
    companyBillingCurrentPlan: "Current plan",
    companyBillingStatus: "Status",
    companyBillingNextCharge: "Next charge",
    companyBillingMethod: "Payment method",
    companyBillingLastPaid: "Last payment",
    companyBillingNoPlan: "No active plan yet",
    companyBillingNoPlanDesc: "Add a billing plan to keep your company account ready for job posting and applicant management.",
    companyBillingActive: "Active",
    companyBillingInactive: "Not set",
    companyPaymentTitle: "Company Payment",
    companyPaymentDesc: "Pick a plan for your company and save a demo card if your plan needs billing.",
    companyPaymentHeroBadge: "Company Billing",
    companyPaymentPlansTitle: "Choose Your Plan",
    companyPaymentFormTitle: "Billing Details",
    companyPaymentSummaryTitle: "Order Summary",
    companyPaymentSubmit: "Confirm Payment",
    companyPaymentSuccess: "Plan saved successfully.",
    companyPaymentSecureNote: "Demo only. Paid plan details stay in this browser and only the last 4 digits are displayed later.",
    companyPaymentCompanyName: "Company Name",
    companyPaymentInvoiceEmail: "Billing Email",
    companyPaymentCardName: "Name on Card",
    companyPaymentCardNumber: "Card Number",
    companyPaymentExpiry: "Expiry (MM/YY)",
    companyPaymentCvv: "CVV",
    companyPaymentMonthly: "Monthly",
    companyPaymentPerMonth: "/ month",
    companyPaymentFree: "Free",
    companyPaymentSelected: "Selected",
    companyPaymentSelect: "Select Plan",
    companyPaymentSummaryHint: "Review your selected plan before you confirm payment.",
    companyPaymentAccessDenied: "This page is for company accounts only.",
    companyPaymentRequired: "Please complete all billing fields and select a plan.",
    companyPaymentCardInvalid: "Please enter a valid card number, expiry date, and CVV.",
    companyBillingPostsUsed: "Posts used",
    companyPostLimitReached: "Your free plan allows up to 3 active job posts. Upgrade your plan to post more jobs.",
    companyPaymentPlanIncludes: "Plan includes",
    companyPlanFree: "Free",
    companyPlanStarter: "Starter",
    companyPlanGrowth: "Growth",
    companyPlanScale: "Scale",
    companyPlanFreeDesc: "A free plan for companies starting small.",
    companyPlanStarterDesc: "Best for new employers starting to post roles.",
    companyPlanGrowthDesc: "For active teams hiring across multiple openings.",
    companyPlanScaleDesc: "For companies that want maximum reach and support.",
    companyPlanFeaturePosts3: "Up to 3 active job posts",
    companyPlanFeaturePosts10: "Up to 10 active job posts",
    companyPlanFeaturePostsUnlimited: "Unlimited active job posts",
    companyPlanFeatureApplicants: "Applicant review dashboard",
    companyPlanFeatureFeatured: "Featured company visibility",
    companyPlanFeatureSupport: "Priority support and billing help",
    companyPlanFeatureAnalytics: "Recruitment insights summary",
    companyPlanFeatureBranding: "Stronger employer branding"
  });

  Object.assign(I18N.ar, {
    userDashTitle: "\u062A\u0642\u062F\u064A\u0645\u0627\u062A\u064A",
    userDashDesc: "\u062A\u0627\u0628\u0639\u064A \u0643\u0644 \u0627\u0644\u0648\u0638\u0627\u0626\u0641 \u0627\u0644\u062A\u064A \u0642\u062F\u0645\u062A\u064A \u0639\u0644\u064A\u0647\u0627 \u0641\u064A \u0645\u0643\u0627\u0646 \u0648\u0627\u062D\u062F.",
    companyDashTitle: "\u0644\u0648\u062D\u0629 \u0627\u0644\u0634\u0631\u0643\u0629",
    companyDashDesc: "\u0623\u0636\u064A\u0641\u064A \u0648\u0638\u0627\u0626\u0641 \u0648\u0631\u0627\u062C\u0639\u064A \u0627\u0644\u0645\u062A\u0642\u062F\u0645\u064A\u0646 (\u062A\u062C\u0631\u064A\u0628\u064A \u064A\u0639\u062A\u0645\u062F \u0639\u0644\u0649 localStorage).",
    jobTitle: "\u0627\u0644\u0639\u0646\u0648\u0627\u0646",
    publish: "\u0646\u0634\u0631",
    companyNote: "(\u062A\u062C\u0631\u064A\u0628\u064A) \u064A\u062A\u0645 \u062D\u0641\u0638 \u0627\u0644\u0648\u0638\u0627\u0626\u0641 \u0627\u0644\u0645\u0646\u0634\u0648\u0631\u0629 \u0641\u064A localStorage.",
    emptyTitle: "\u0644\u0627 \u062A\u0648\u062C\u062F \u0646\u062A\u0627\u0626\u062C",
    emptyDesc: "\u062C\u0631\u0628\u064A \u062A\u063A\u064A\u064A\u0631 \u0627\u0644\u0641\u0644\u0627\u062A\u0631 \u0623\u0648 \u0643\u0644\u0645\u0627\u062A \u0627\u0644\u0628\u062D\u062B.",
    cvNote: "(\u062A\u062C\u0631\u064A\u0628\u064A) \u064A\u062A\u0645 \u062D\u0641\u0638 \u0627\u0644\u0637\u0644\u0628\u0627\u062A \u0645\u062D\u0644\u064A\u064B\u0627 \u0641\u064A \u0647\u0630\u0627 \u0627\u0644\u0645\u062A\u0635\u0641\u062D.",
    cvBuilderCta: "\u0628\u0646\u0627\u0621 \u0627\u0644\u0633\u064A\u0631\u0629 \u0627\u0644\u0630\u0627\u062A\u064A\u0629",
    cvBuilderCtaTitle: "\u0627\u0646\u0634\u0626\u064A \u0633\u064A\u0631\u0629 \u0630\u0627\u062A\u064A\u0629 \u062C\u0627\u0647\u0632\u0629 \u0644\u0644\u0648\u0638\u0627\u0626\u0641 \u0641\u064A \u062F\u0642\u0627\u0626\u0642",
    cvBuilderCtaDesc: "\u0627\u062D\u0641\u0638\u064A \u0633\u064A\u0631\u062A\u0643 \u0627\u0644\u0630\u0627\u062A\u064A\u0629 \u062F\u0627\u062E\u0644 SJB \u0648\u0627\u0633\u062A\u062E\u062F\u0645\u064A\u0647\u0627 \u0639\u0646\u062F \u0627\u0644\u062A\u0642\u062F\u064A\u0645 \u0639\u0644\u0649 \u0648\u0638\u0627\u0626\u0641 \u0627\u0644\u0637\u0644\u0627\u0628.",
    openCvBuilder: "\u0627\u0641\u062A\u062D\u064A \u0628\u0646\u0627\u0621 \u0627\u0644\u0633\u064A\u0631\u0629",
    cvBuilderTitle: "\u0628\u0646\u0627\u0621 \u0627\u0644\u0633\u064A\u0631\u0629 \u0627\u0644\u0630\u0627\u062A\u064A\u0629",
    cvBuilderDesc: "\u0627\u0628\u0646\u064A \u0633\u064A\u0631\u0629 \u0630\u0627\u062A\u064A\u0629 \u0645\u0631\u062A\u0628\u0629\u060C \u0627\u062D\u0641\u0638\u064A\u0647\u0627 \u0645\u062D\u0644\u064A\u064B\u0627 \u0641\u064A SJB\u060C \u062B\u0645 \u062D\u0645\u0644\u064A\u0647\u0627 \u0643\u0645\u0644\u0641 PDF \u0639\u0646\u062F\u0645\u0627 \u062A\u0643\u0648\u0646 \u062C\u0627\u0647\u0632\u0629.",
    cvBuilderFormTitle: "\u0627\u0628\u0646\u064A \u0628\u064A\u0627\u0646\u0627\u062A\u0643",
    cvBuilderPreviewTitle: "\u0645\u0639\u0627\u064A\u0646\u0629 \u0641\u0648\u0631\u064A\u0629 \u0644\u0644\u0633\u064A\u0631\u0629",
    cvPreviewHint: "\u0627\u0633\u062A\u062E\u062F\u0645\u064A \u0632\u0631 \u062A\u062D\u0645\u064A\u0644 PDF \u0644\u062D\u0641\u0638 \u0633\u064A\u0631\u0629 \u0630\u0627\u062A\u064A\u0629 \u0645\u062A\u0639\u062F\u062F\u0629 \u0627\u0644\u0635\u0641\u062D\u0627\u062A \u0645\u0628\u0627\u0634\u0631\u0629 \u0645\u0646 \u0627\u0644\u0628\u0646\u0627\u0621.",
    cvTargetRole: "\u0627\u0644\u0645\u0633\u0645\u0649 \u0627\u0644\u0645\u0633\u062A\u0647\u062F\u0641",
    cvSummary: "\u0627\u0644\u0645\u0644\u062E\u0635 \u0627\u0644\u0645\u0647\u0646\u064A",
    cvSkills: "\u0627\u0644\u0645\u0647\u0627\u0631\u0627\u062A",
    cvEducation: "\u0627\u0644\u062A\u0639\u0644\u064A\u0645",
    cvExperience: "\u0627\u0644\u062E\u0628\u0631\u0629",
    cvProjects: "\u0627\u0644\u0645\u0634\u0627\u0631\u064A\u0639",
    cvLanguages: "\u0627\u0644\u0644\u063A\u0627\u062A",
    cvWebsite: "\u0627\u0644\u0645\u0648\u0642\u0639 \u0623\u0648 \u0645\u0639\u0631\u0636 \u0627\u0644\u0623\u0639\u0645\u0627\u0644",
    cvLinkedIn: "LinkedIn",
    cvSaveDraft: "\u0627\u062D\u0641\u0638\u064A \u0627\u0644\u0645\u0633\u0648\u062F\u0629",
    cvReset: "\u0625\u0639\u0627\u062F\u0629 \u0636\u0628\u0637 \u0627\u0644\u0633\u064A\u0631\u0629",
    cvExportPdf: "\u062A\u062D\u0645\u064A\u0644 PDF",
    cvAddEducation: "\u0625\u0636\u0627\u0641\u0629 \u062A\u0639\u0644\u064A\u0645",
    cvAddExperience: "\u0625\u0636\u0627\u0641\u0629 \u062E\u0628\u0631\u0629",
    cvAddProject: "\u0625\u0636\u0627\u0641\u0629 \u0645\u0634\u0631\u0648\u0639",
    cvNoCvYet: "\u0644\u0627 \u062A\u0648\u062C\u062F \u0633\u064A\u0631\u0629 \u0645\u062D\u0641\u0648\u0638\u0629 \u0628\u0639\u062F",
    cvNoCvDesc: "\u0627\u0628\u062F\u0623\u064A \u0633\u064A\u0631\u062A\u0643 \u0627\u0644\u0630\u0627\u062A\u064A\u0629 \u0627\u0644\u0622\u0646 \u0644\u062A\u0633\u062A\u062E\u062F\u0645\u064A\u0647\u0627 \u0645\u0631\u0629 \u0623\u062E\u0631\u0649 \u0639\u0646\u062F \u0627\u0644\u062A\u0642\u062F\u064A\u0645.",
    cvSaved: "\u0633\u064A\u0631\u0629 \u0645\u062D\u0641\u0648\u0638\u0629",
    cvLastUpdated: "\u0622\u062E\u0631 \u062A\u062D\u062F\u064A\u062B",
    useSavedCv: "\u0627\u0633\u062A\u062E\u062F\u0645\u064A \u0633\u064A\u0631\u062A\u064A \u0627\u0644\u0645\u062D\u0641\u0648\u0638\u0629 \u0641\u064A SJB",
    useSavedCvDesc: "\u0633\u0646\u0631\u0641\u0642 \u0627\u0644\u0633\u064A\u0631\u0629 \u0627\u0644\u062A\u064A \u0628\u0646\u064A\u062A\u0647\u0627 \u062F\u0627\u062E\u0644 SJB \u0645\u0639 \u0647\u0630\u0627 \u0627\u0644\u0637\u0644\u0628.",
    buildCvInstead: "\u0627\u0628\u0646\u064A \u0633\u064A\u0631\u0629 \u0623\u0648\u0644\u0627\u064B",
    cvSourceBuilder: "\u0633\u064A\u0631\u0629 SJB",
    cvApplyRequired: "\u0645\u0646 \u0641\u0636\u0644\u0643 \u0627\u0643\u062A\u0628\u064A \u0643\u0644 \u0627\u0644\u0628\u064A\u0627\u0646\u0627\u062A \u0648\u0627\u0631\u0641\u0639\u064A \u0633\u064A\u0631\u0629 \u0623\u0648 \u0627\u0633\u062A\u062E\u062F\u0645\u064A \u0633\u064A\u0631\u062A\u0643 \u0627\u0644\u0645\u062D\u0641\u0648\u0638\u0629.",
    cvDraftSaved: "\u062A\u0645 \u062D\u0641\u0638 \u0627\u0644\u0645\u0633\u0648\u062F\u0629 \u0628\u0646\u062C\u0627\u062D.",
    cvDraftReset: "\u062A\u0645\u062A \u0625\u0639\u0627\u062F\u0629 \u0636\u0628\u0637 \u0627\u0644\u0645\u0633\u0648\u062F\u0629.",
    cvResetConfirm: "\u0647\u0644 \u062A\u0631\u064A\u062F\u064A\u0646 \u0625\u0639\u0627\u062F\u0629 \u0636\u0628\u0637 \u0645\u0633\u0648\u062F\u0629 \u0627\u0644\u0633\u064A\u0631\u0629\u061F",
    companyBilling: "\u0627\u0644\u062F\u0641\u0639",
    companyBillingTitle: "\u0627\u0644\u0628\u0627\u0642\u0629 \u0648\u0627\u0644\u062F\u0641\u0639",
    companyBillingDesc: "\u0627\u062E\u062A\u0627\u0631\u064A \u0628\u0627\u0642\u0629\u060C \u0648\u0627\u062D\u0641\u0638\u064A \u0637\u0631\u064A\u0642\u0629 \u062F\u0641\u0639 \u062A\u062C\u0631\u064A\u0628\u064A\u0629\u060C \u0648\u062E\u0644\u064A \u062D\u0633\u0627\u0628 \u0634\u0631\u0643\u062A\u0643 \u062C\u0627\u0647\u0632 \u0644\u0644\u062A\u0648\u0638\u064A\u0641.",
    companyBillingManage: "\u0625\u062F\u0627\u0631\u0629 \u0627\u0644\u062F\u0641\u0639",
    companyBillingOpen: "\u0627\u0641\u062A\u062D\u064A \u0627\u0644\u062F\u0641\u0639",
    companyBillingCurrentPlan: "\u0627\u0644\u0628\u0627\u0642\u0629 \u0627\u0644\u062D\u0627\u0644\u064A\u0629",
    companyBillingStatus: "\u0627\u0644\u062D\u0627\u0644\u0629",
    companyBillingNextCharge: "\u0627\u0644\u062F\u0641\u0639\u0629 \u0627\u0644\u0642\u0627\u062F\u0645\u0629",
    companyBillingMethod: "\u0637\u0631\u064A\u0642\u0629 \u0627\u0644\u062F\u0641\u0639",
    companyBillingLastPaid: "\u0622\u062E\u0631 \u062F\u0641\u0639\u0629",
    companyBillingNoPlan: "\u0644\u0627 \u062A\u0648\u062C\u062F \u0628\u0627\u0642\u0629 \u0646\u0634\u0637\u0629 \u0628\u0639\u062F",
    companyBillingNoPlanDesc: "\u0623\u0636\u064A\u0641\u064A \u0628\u0627\u0642\u0629 \u062F\u0641\u0639 \u0644\u064A\u0643\u0648\u0646 \u062D\u0633\u0627\u0628 \u0634\u0631\u0643\u062A\u0643 \u062C\u0627\u0647\u0632 \u0644\u0646\u0634\u0631 \u0627\u0644\u0648\u0638\u0627\u0626\u0641 \u0648\u0645\u0631\u0627\u062C\u0639\u0629 \u0627\u0644\u0645\u062A\u0642\u062F\u0645\u064A\u0646.",
    companyBillingActive: "\u0646\u0634\u0637\u0629",
    companyBillingInactive: "\u063A\u064A\u0631 \u0645\u0636\u0628\u0648\u0637\u0629",
    companyPaymentTitle: "\u062F\u0641\u0639 \u0627\u0644\u0634\u0631\u0643\u0629",
    companyPaymentDesc: "\u0627\u062E\u062A\u0627\u0631\u064A \u0628\u0627\u0642\u0629 \u0644\u0634\u0631\u0643\u062A\u0643 \u0648\u0627\u062D\u0641\u0638\u064A \u0628\u0637\u0627\u0642\u0629 \u062A\u062C\u0631\u064A\u0628\u064A\u0629 \u0625\u0630\u0627 \u0643\u0627\u0646\u062A \u0627\u0644\u0628\u0627\u0642\u0629 \u062A\u062D\u062A\u0627\u062C \u062F\u0641\u0639\u064B\u0627.",
    companyPaymentHeroBadge: "\u062F\u0641\u0639 \u0627\u0644\u0634\u0631\u0643\u0629",
    companyPaymentPlansTitle: "\u0627\u062E\u062A\u0627\u0631\u064A \u0628\u0627\u0642\u062A\u0643",
    companyPaymentFormTitle: "\u0628\u064A\u0627\u0646\u0627\u062A \u0627\u0644\u062F\u0641\u0639",
    companyPaymentSummaryTitle: "\u0645\u0644\u062E\u0635 \u0627\u0644\u0637\u0644\u0628",
    companyPaymentSubmit: "\u062A\u0623\u0643\u064A\u062F \u0627\u0644\u062F\u0641\u0639",
    companyPaymentSuccess: "\u062A\u0645 \u062D\u0641\u0638 \u0627\u0644\u0628\u0627\u0642\u0629 \u0628\u0646\u062C\u0627\u062D.",
    companyPaymentSecureNote: "\u062A\u062C\u0631\u064A\u0628\u064A \u0641\u0642\u0637. \u0628\u064A\u0627\u0646\u0627\u062A \u0627\u0644\u062F\u0641\u0639 \u0644\u0644\u0628\u0627\u0642\u0627\u062A \u0627\u0644\u0645\u062F\u0641\u0648\u0639\u0629 \u062A\u0628\u0642\u0649 \u0641\u064A \u0647\u0630\u0627 \u0627\u0644\u0645\u062A\u0635\u0641\u062D \u0648\u064A\u0638\u0647\u0631 \u0641\u0642\u0637 \u0622\u062E\u0631 4 \u0623\u0631\u0642\u0627\u0645.",
    companyPaymentCompanyName: "\u0627\u0633\u0645 \u0627\u0644\u0634\u0631\u0643\u0629",
    companyPaymentInvoiceEmail: "\u0628\u0631\u064A\u062F \u0627\u0644\u0641\u0648\u0627\u062A\u064A\u0631",
    companyPaymentCardName: "\u0627\u0644\u0627\u0633\u0645 \u0639\u0644\u0649 \u0627\u0644\u0628\u0637\u0627\u0642\u0629",
    companyPaymentCardNumber: "\u0631\u0642\u0645 \u0627\u0644\u0628\u0637\u0627\u0642\u0629",
    companyPaymentExpiry: "\u062A\u0627\u0631\u064A\u062E \u0627\u0644\u0627\u0646\u062A\u0647\u0627\u0621 (MM/YY)",
    companyPaymentCvv: "CVV",
    companyPaymentMonthly: "\u0634\u0647\u0631\u064A",
    companyPaymentPerMonth: "/ \u0634\u0647\u0631",
    companyPaymentFree: "\u0645\u062C\u0627\u0646\u0627\u064B",
    companyPaymentSelected: "\u0645\u062E\u062A\u0627\u0631\u0629",
    companyPaymentSelect: "\u0627\u062E\u062A\u0631 \u0627\u0644\u0628\u0627\u0642\u0629",
    companyPaymentSummaryHint: "\u0631\u0627\u062C\u0639\u064A \u0628\u0627\u0642\u062A\u0643 \u0627\u0644\u0645\u062E\u062A\u0627\u0631\u0629 \u0642\u0628\u0644 \u062A\u0623\u0643\u064A\u062F \u0627\u0644\u062F\u0641\u0639.",
    companyPaymentAccessDenied: "\u0647\u0630\u0647 \u0627\u0644\u0635\u0641\u062D\u0629 \u0644\u062D\u0633\u0627\u0628\u0627\u062A \u0627\u0644\u0634\u0631\u0643\u0627\u062A \u0641\u0642\u0637.",
    companyPaymentRequired: "\u0645\u0646 \u0641\u0636\u0644\u0643 \u0627\u0643\u0645\u0644\u064A \u0643\u0644 \u0628\u064A\u0627\u0646\u0627\u062A \u0627\u0644\u062F\u0641\u0639 \u0648\u0627\u062E\u062A\u0627\u0631\u064A \u0628\u0627\u0642\u0629.",
    companyPaymentCardInvalid: "\u0645\u0646 \u0641\u0636\u0644\u0643 \u0623\u062F\u062E\u0644\u064A \u0631\u0642\u0645 \u0628\u0637\u0627\u0642\u0629 \u0648\u062A\u0627\u0631\u064A\u062E \u0627\u0646\u062A\u0647\u0627\u0621 \u0648CVV \u0635\u062D\u064A\u062D\u064A\u0646.",
    companyBillingPostsUsed: "\u0627\u0644\u0648\u0638\u0627\u0626\u0641 \u0627\u0644\u0645\u0633\u062A\u062E\u062F\u0645\u0629",
    companyPostLimitReached: "\u0627\u0644\u0628\u0627\u0642\u0629 \u0627\u0644\u0645\u062C\u0627\u0646\u064A\u0629 \u062A\u0633\u0645\u062D \u0628\u062D\u062F \u0623\u0642\u0635\u0649 3 \u0648\u0638\u0627\u0626\u0641 \u0646\u0634\u0637\u0629. \u0631\u0642\u064A \u0628\u0627\u0642\u062A\u0643 \u0644\u0646\u0634\u0631 \u0645\u0632\u064A\u062F \u0645\u0646 \u0627\u0644\u0648\u0638\u0627\u0626\u0641.",
    companyPaymentPlanIncludes: "\u0627\u0644\u0628\u0627\u0642\u0629 \u062A\u0634\u0645\u0644",
    companyPlanFree: "\u0645\u062C\u0627\u0646\u064A\u0629",
    companyPlanStarter: "\u0633\u062A\u0627\u0631\u062A\u0631",
    companyPlanGrowth: "\u062C\u0631\u0648\u062B",
    companyPlanScale: "\u0633\u0643\u064A\u0644",
    companyPlanFreeDesc: "\u0628\u0627\u0642\u0629 \u0645\u062C\u0627\u0646\u064A\u0629 \u0644\u0644\u0634\u0631\u0643\u0627\u062A \u0627\u0644\u062A\u064A \u062A\u0628\u062F\u0623 \u0628\u0634\u0643\u0644 \u0635\u063A\u064A\u0631.",
    companyPlanStarterDesc: "\u0645\u0646\u0627\u0633\u0628\u0629 \u0644\u0644\u0634\u0631\u0643\u0627\u062A \u0627\u0644\u062A\u064A \u062A\u0628\u062F\u0623 \u0641\u064A \u0646\u0634\u0631 \u0648\u0638\u0627\u0626\u0641\u0647\u0627.",
    companyPlanGrowthDesc: "\u0644\u0644\u0641\u0631\u0642 \u0627\u0644\u0646\u0634\u0637\u0629 \u0627\u0644\u062A\u064A \u062A\u0648\u0638\u0641 \u0639\u0644\u0649 \u0623\u0643\u062B\u0631 \u0645\u0646 \u0641\u0631\u0635\u0629.",
    companyPlanScaleDesc: "\u0644\u0644\u0634\u0631\u0643\u0627\u062A \u0627\u0644\u062A\u064A \u062A\u0631\u064A\u062F \u0623\u0642\u0635\u0649 \u0627\u0646\u062A\u0634\u0627\u0631 \u0648\u062F\u0639\u0645.",
    companyPlanFeaturePosts3: "\u062D\u062A\u0649 3 \u0648\u0638\u0627\u0626\u0641 \u0646\u0634\u0637\u0629",
    companyPlanFeaturePosts10: "\u062D\u062A\u0649 10 \u0648\u0638\u0627\u0626\u0641 \u0646\u0634\u0637\u0629",
    companyPlanFeaturePostsUnlimited: "\u0648\u0638\u0627\u0626\u0641 \u0646\u0634\u0637\u0629 \u063A\u064A\u0631 \u0645\u062D\u062F\u0648\u062F\u0629",
    companyPlanFeatureApplicants: "\u0644\u0648\u062D\u0629 \u0645\u0631\u0627\u062C\u0639\u0629 \u0627\u0644\u0645\u062A\u0642\u062F\u0645\u064A\u0646",
    companyPlanFeatureFeatured: "\u0638\u0647\u0648\u0631 \u0645\u0645\u064A\u0632 \u0644\u0644\u0634\u0631\u0643\u0629",
    companyPlanFeatureSupport: "\u062F\u0639\u0645 \u0623\u0648\u0644\u0648\u064A \u0648\u0645\u0633\u0627\u0639\u062F\u0629 \u0641\u064A \u0627\u0644\u062F\u0641\u0639",
    companyPlanFeatureAnalytics: "\u0645\u0644\u062E\u0635 \u0625\u062D\u0635\u0627\u0626\u064A\u0627\u062A \u0627\u0644\u062A\u0648\u0638\u064A\u0641",
    companyPlanFeatureBranding: "\u062A\u0639\u0632\u064A\u0632 \u0647\u0648\u064A\u0629 \u0627\u0644\u0634\u0631\u0643\u0629 \u0643\u0635\u0627\u062D\u0628 \u0639\u0645\u0644"
  });
}

function qs(name){ return new URLSearchParams(window.location.search).get(name); }
function go(url){ window.location.href = url; }

function getRole(){ return localStorage.getItem("sjb_role"); }
function getUser(){
  try { return JSON.parse(localStorage.getItem("sjb_user") || "null"); }
  catch { return null; }
}
function hasText(value){
  return String(value ?? "").trim().length > 0;
}
function escapeHtml(value){
  return String(value ?? "").replace(/[&<>"']/g, (char)=>({
    "&":"&amp;",
    "<":"&lt;",
    ">":"&gt;",
    "\"":"&quot;",
    "'":"&#39;"
  }[char]));
}
function getSavedCVBuilder(){
  try { return JSON.parse(localStorage.getItem("sjb_saved_cv") || "null"); }
  catch { return null; }
}
function cvCollectionHasContent(list){
  return Array.isArray(list) && list.some(item =>
    Object.values(item || {}).some(value => hasText(value))
  );
}
function hasSavedCVBuilder(){
  const cv = getSavedCVBuilder();
  if(!cv) return false;

  const basicFields = [
    cv.fullName, cv.role, cv.email, cv.phone, cv.location,
    cv.website, cv.summary, cv.skills, cv.languages
  ];

  return basicFields.some(hasText)
    || cvCollectionHasContent(cv.education)
    || cvCollectionHasContent(cv.experience)
    || cvCollectionHasContent(cv.projects);
}
function formatSavedCVDate(value){
  if(!value) return "-";
  const date = new Date(value);
  if(Number.isNaN(date.getTime())) return "-";

  return date.toLocaleDateString(getLang()==="ar" ? "ar-EG" : "en-US", {
    year: "numeric",
    month: "short",
    day: "numeric"
  });
}
function applicationCVText(app){
  if(app?.cvSource === "builder") return t("cvSourceBuilder");
  return app?.cvName || "-";
}
function getCompanyBillingStorage(){
  try { return JSON.parse(localStorage.getItem("sjb_company_billing") || "{}"); }
  catch { return {}; }
}
function getCurrentCompanyBillingKey(){
  const user = getUser();
  return norm(user?.email || "");
}
function getCurrentCompanyBilling(){
  const key = getCurrentCompanyBillingKey();
  if(!key) return null;

  const store = getCompanyBillingStorage();
  return store[key] || null;
}
function saveCurrentCompanyBilling(record){
  const key = getCurrentCompanyBillingKey();
  if(!key) return null;

  const store = getCompanyBillingStorage();
  store[key] = record;
  localStorage.setItem("sjb_company_billing", JSON.stringify(store));
  return record;
}
function getAllCompanyJobs(){
  try { return JSON.parse(localStorage.getItem("sjb_company_jobs") || "[]"); }
  catch { return []; }
}
function saveAllCompanyJobs(jobs){
  localStorage.setItem("sjb_company_jobs", JSON.stringify(jobs));
}
function getCurrentCompanyJobs(){
  const key = getCurrentCompanyBillingKey();
  return getAllCompanyJobs().filter(job => !key || !job.ownerKey || job.ownerKey === key);
}
function getCompanyPlanCatalog(){
  return [
    {
      id: "free",
      price: 0,
      maxPosts: 3,
      name: t("companyPlanFree"),
      description: t("companyPlanFreeDesc"),
      features: [
        t("companyPlanFeaturePosts3"),
        t("companyPlanFeatureApplicants"),
        t("companyPlanFeatureBranding")
      ]
    },
    {
      id: "growth",
      price: 1499,
      maxPosts: 10,
      name: t("companyPlanGrowth"),
      description: t("companyPlanGrowthDesc"),
      features: [
        t("companyPlanFeaturePosts10"),
        t("companyPlanFeatureFeatured"),
        t("companyPlanFeatureSupport")
      ]
    },
    {
      id: "scale",
      price: 3499,
      maxPosts: Number.POSITIVE_INFINITY,
      name: t("companyPlanScale"),
      description: t("companyPlanScaleDesc"),
      features: [
        t("companyPlanFeaturePostsUnlimited"),
        t("companyPlanFeatureAnalytics"),
        t("companyPlanFeatureSupport")
      ]
    }
  ];
}
function getCompanyPlanById(planId){
  return getCompanyPlanCatalog().find(plan => plan.id === planId) || null;
}
function getCurrentCompanyPlan(){
  return getCompanyPlanById(getCurrentCompanyBilling()?.planId) || getCompanyPlanById("free") || getCompanyPlanCatalog()[0];
}
function formatBillingDate(value){
  if(!value) return "-";
  const date = new Date(value);
  if(Number.isNaN(date.getTime())) return "-";

  return date.toLocaleDateString(getLang()==="ar" ? "ar-EG" : "en-US", {
    year: "numeric",
    month: "short",
    day: "numeric"
  });
}
function formatPlanPrice(value){
  const amount = Number(value) || 0;
  if(amount === 0) return t("companyPaymentFree");

  const locale = getLang()==="ar" ? "ar-EG" : "en-US";
  const formatted = amount.toLocaleString(locale);
  return getLang()==="ar" ? `${formatted} ج.م` : `EGP ${formatted}`;
}
function logout(){
  localStorage.removeItem("sjb_role");
  localStorage.removeItem("sjb_user");
  go("index.html");
}

function initNavActive(){
  const page = location.pathname.split("/").pop() || "index.html";
  const role = getRole();
  const isLoggedIn = Boolean(role && getUser());

  document.querySelectorAll(".nav a").forEach(link => {
    if(link.getAttribute("href") === page) link.classList.add("active");
  });

  const dash = document.getElementById("navDash");
  if(dash){
    if(!role){
      dash.style.display = "none";
    }else{
      dash.style.display = "inline-flex";
      dash.setAttribute("href",
        role === "user" ? "dashboard-user.html" :
        role === "company" ? "dashboard-company.html" :
        "dashboard-admin.html"
      );
    }
  }

  document.querySelectorAll("[data-auth-guest]").forEach(element => {
    element.style.display = isLoggedIn ? "none" : "";
  });

  document.querySelectorAll("[data-auth-user]").forEach(element => {
    if(!isLoggedIn){
      element.style.display = "none";
      return;
    }

    element.style.display = "";
    if(element.tagName === "A"){
      element.setAttribute("href",
        role === "user" ? "dashboard-user.html" :
        role === "company" ? "dashboard-company.html" :
        "dashboard-admin.html"
      );
    }
  });

  const loginLink = document.getElementById("navLogin");
  if(loginLink && role){
    loginLink.textContent = t("logout");
    loginLink.addEventListener("click", (event)=>{
      event.preventDefault();
      logout();
    });
  }
}

function initLanguageButton(){
  const button = document.getElementById("langBtn");
  if(!button) return;

  button.textContent = (getLang() === "ar") ? "EN" : "AR";

  button.addEventListener("click", ()=>{
    const next = (getLang() === "ar") ? "en" : "ar";
    setLang(next);
    button.textContent = (next === "ar") ? "EN" : "AR";

    renderJobs();
    renderJobDetails();
    renderUserDashboard();
    renderCompanyDashboard();
    renderAdminDashboard();
    if(typeof window.refreshCVBuilderCopy === "function") window.refreshCVBuilderCopy();
    if(typeof window.refreshCompanyPaymentCopy === "function") window.refreshCompanyPaymentCopy();
  });
}

function norm(value){
  return String(value ?? "").trim().toLowerCase();
}

function locationText(raw){
  const value = norm(raw);
  if(value === "cairo") return t("locCairo");
  if(value === "alexandria") return t("locAlex");
  if(value === "giza") return t("locGiza");
  if(value === "dubai") return t("locDubai");
  if(value === "riyadh") return t("locRiyadh");
  if(value === "doha") return t("locDoha");
  if(value === "london") return t("locLondon");
  if(value === "berlin") return t("locBerlin");
  if(value === "toronto") return t("locToronto");
  if(value === "newyork" || value === "new york") return t("locNewYork");
  if(value === "remote") return t("locRemote");
  return String(raw || "");
}

function typeText(raw){
  const value = norm(raw).replace(/\s/g, "");
  if(value === "fulltime") return t("typeFull");
  if(value === "parttime") return t("typePart");
  if(value === "internship") return t("typeIntern");
  return String(raw || "");
}

function renderJobs(){
  const grid = document.querySelector("main #jobsGrid") || document.getElementById("jobsGrid");
  if(!grid) return;

  const lang = getLang();
  const loc = document.getElementById("locSelect")?.value || "all";
  const type = document.getElementById("typeSelect")?.value || "all";

  const jobs = (window.SJB_JOBS || []).filter(job => {
    const locationOk = (loc === "all") || (norm(job.location) === norm(loc));
    const typeOk = (type === "all") || (norm(job.type).replace(/\s/g, "") === norm(type));
    return locationOk && typeOk;
  });

  if(!jobs.length){
    grid.innerHTML = `
      <div class="panel" style="grid-column:1/-1;text-align:center">
        <b>${lang === "ar" ? "\u0644\u0627 \u062A\u0648\u062C\u062F \u0648\u0638\u0627\u0626\u0641" : "No jobs found"}</b>
      </div>
    `;
    return;
  }

  grid.innerHTML = jobs.map(job => {
    const title = job.title?.[lang] || job.title?.en || "";
    const company = job.company?.[lang] || job.company?.en || "";

    return `
      <div class="card">
        <div class="logoBadge"><span>${escapeHtml(company.trim().slice(0, 1) || "J")}</span></div>
        <div>
          <h3>${escapeHtml(title)}</h3>
          <div class="company">${escapeHtml(company)}</div>
          <div class="meta">
            <span class="pill green">${escapeHtml(locationText(job.location))}</span>
            <span class="pill">${escapeHtml(typeText(job.type))}</span>
          </div>
        </div>
        <div class="right">
          <div class="salary">${escapeHtml(job.salary || "")}</div>
          <button class="btn accent" onclick="go('job.html?id=${job.id}')">${t("viewDetails")}</button>
        </div>
      </div>
    `;
  }).join("");
}

function renderJobDetails(){
  const wrap = document.getElementById("jobDetailsWrap");
  if(!wrap) return;

  const id = qs("id");
  const job = (window.SJB_JOBS || []).find(item => String(item.id) === String(id));

  if(!id || !job){
    wrap.innerHTML = `
      <div class="panel" style="text-align:center">
        <b>${t("missingIdTitle")}</b>
        <div class="small" style="margin-top:8px">${t("missingIdDesc")}</div>
        <div style="margin-top:12px">
          <button class="btn accent" onclick="go('jobs.html')">${t("backJobs")}</button>
        </div>
      </div>
    `;
    return;
  }

  const lang = getLang();
  const title = job.title?.[lang] || job.title?.en || "";
  const company = job.company?.[lang] || job.company?.en || "";
  const description = job.description?.[lang] || job.description?.en || "";
  const requirements = Array.isArray(job.requirements?.[lang])
    ? job.requirements[lang]
    : (Array.isArray(job.requirements?.en) ? job.requirements.en : []);

  wrap.innerHTML = `
    <div class="panel">
      <div style="display:flex;justify-content:space-between;gap:12px;flex-wrap:wrap;align-items:center">
        <div>
          <div class="badgeRole">${escapeHtml(company)}</div>
          <h2 style="margin:10px 0 0">${escapeHtml(title)}</h2>
          <div class="meta" style="margin-top:10px">
            <span class="pill green">${escapeHtml(locationText(job.location))}</span>
            <span class="pill">${escapeHtml(typeText(job.type))}</span>
            <span class="pill">${escapeHtml(job.salary || "")}</span>
          </div>
        </div>
        <div class="stack">
          <button class="btn outline" onclick="go('jobs.html')">${t("backJobs")}</button>
          <button class="btn accent" onclick="openApply(${job.id})">${t("applyNow")}</button>
        </div>
      </div>
      <div class="hr"></div>
      <h3>${t("description")}</h3>
      <p style="line-height:1.8;margin-top:8px;font-weight:700;color:#334155">${escapeHtml(description)}</p>
      <h3 style="margin-top:16px">${t("requirements")}</h3>
      <ul style="margin-top:8px;font-weight:700;color:#334155;line-height:1.9">
        ${requirements.map(item => `<li>${escapeHtml(item)}</li>`).join("")}
      </ul>
    </div>
  `;
}

function syncSavedCVOption(){
  const fileInput = document.getElementById("appCv");
  if(!fileInput) return;

  const optionWrap = document.getElementById("savedCvOption");
  const useSaved = document.getElementById("useSavedCv");
  const savedExists = hasSavedCVBuilder();

  if(optionWrap) optionWrap.style.display = savedExists ? "block" : "none";
  if(!savedExists && useSaved) useSaved.checked = false;

  const usingBuilderCV = Boolean(useSaved?.checked) && savedExists;
  fileInput.disabled = usingBuilderCV;
  fileInput.required = !usingBuilderCV;
}

function openApply(jobId){
  const role = getRole();
  if(!role){
    localStorage.setItem("sjb_redirect", location.href);
    go("auth.html");
    return;
  }

  if(role !== "user"){
    alert(getLang()==="ar"
      ? "\u0627\u0644\u062A\u0642\u062F\u064A\u0645 \u0645\u062A\u0627\u062D \u0644\u0644\u0637\u0644\u0627\u0628 \u0641\u0642\u0637"
      : "Apply is for students/users only");
    return;
  }

  const overlay = document.getElementById("applyOverlay");
  if(!overlay) return;

  overlay.style.display = "flex";
  document.getElementById("applyJobId").value = String(jobId);

  const user = getUser();
  if(user){
    document.getElementById("appName").value = user.name || "";
    document.getElementById("appEmail").value = user.email || "";
  }

  syncSavedCVOption();
}

function closeApply(){
  const overlay = document.getElementById("applyOverlay");
  if(overlay) overlay.style.display = "none";

  const fileInput = document.getElementById("appCv");
  if(fileInput) fileInput.value = "";

  const useSaved = document.getElementById("useSavedCv");
  if(useSaved) useSaved.checked = false;

  syncSavedCVOption();
}

function submitApplication(event){
  event.preventDefault();

  if(getRole() !== "user"){
    alert(getLang()==="ar"
      ? "\u0645\u0633\u0645\u0648\u062D \u0644\u0644\u0637\u0644\u0627\u0628 \u0641\u0642\u0637"
      : "Only for users");
    return;
  }

  const uploadedCvName = document.getElementById("appCv").files[0]?.name || "";
  const useBuilderCV = Boolean(document.getElementById("useSavedCv")?.checked) && hasSavedCVBuilder();
  const savedCV = useBuilderCV ? getSavedCVBuilder() : null;

  const application = {
    id: Date.now(),
    jobId: Number(document.getElementById("applyJobId").value),
    name: document.getElementById("appName").value.trim(),
    email: document.getElementById("appEmail").value.trim(),
    phone: document.getElementById("appPhone").value.trim(),
    cvName: useBuilderCV ? "SJB CV Builder" : uploadedCvName,
    cvSource: useBuilderCV ? "builder" : "upload"
  };

  if(savedCV) application.cvBuilderSnapshot = savedCV;

  if(!application.name || !application.email || !application.phone || !application.cvName){
    alert(t("cvApplyRequired"));
    return;
  }

  const applications = JSON.parse(localStorage.getItem("sjb_apps") || "[]");
  applications.push(application);
  localStorage.setItem("sjb_apps", JSON.stringify(applications));

  alert(getLang()==="ar"
    ? "\u062A\u0645 \u0625\u0631\u0633\u0627\u0644 \u0637\u0644\u0628\u0643 \u0628\u0646\u062C\u0627\u062D"
    : "Application submitted successfully");
  closeApply();
  go("dashboard-user.html");
}

function authInit(){
  const form = document.getElementById("authForm");
  if(!form) return;

  const roleButtons = document.querySelectorAll("[data-role]");
  const roleInput = document.getElementById("authRole");
  const roleLabel = document.getElementById("roleLabel");
  const authMode = form.dataset.authMode || "signin";
  const isSignupMode = authMode === "signup";

  function locationField(){
    return document.getElementById("locationField") || document.getElementById("locField");
  }

  function extraField(){
    return document.getElementById("extraInfoField");
  }

  function extraLabel(){
    return document.getElementById("extraInfoLabel");
  }

  function extraInput(){
    return document.getElementById("authExtraInfo");
  }

  function phoneInput(){
    return document.getElementById("authPhone");
  }

  function updateSignupCopy(role){
    const nameInput = document.getElementById("authName");
    const label = extraLabel();
    const input = extraInput();
    if(!isSignupMode) return;

    if(nameInput){
      nameInput.placeholder =
        role === "company" ? "Company Name" :
        role === "admin" ? "Admin Name" :
        "Full Name";
    }

    if(label){
      label.textContent =
        role === "company" ? "Industry / Website" :
        role === "admin" ? "Department / Admin Title" :
        "University / Major";
    }

    if(input){
      input.placeholder =
        role === "company" ? "Technology, hiring team, or company website" :
        role === "admin" ? "Career services, operations, or admin title" :
        "Cairo University - Computer Science";
    }
  }

  function toggleLocationField(role){
    const field = locationField();
    if(!field) return;
    field.style.display = (isSignupMode || role === "company") ? "flex" : "none";
  }

  function toggleExtraField(role){
    const field = extraField();
    if(!field) return;
    field.style.display = isSignupMode ? "flex" : "none";
    updateSignupCopy(role);
  }

  roleButtons.forEach((button, index)=>{
    if(index === 0 && !button.classList.contains("primary")) button.classList.add("primary");

    button.addEventListener("click", ()=>{
      roleButtons.forEach(item => item.classList.remove("primary"));
      button.classList.add("primary");

      const role = button.dataset.role || "user";
      roleInput.value = role;
      roleLabel.textContent =
        role === "user" ? t("roleStudent") :
        role === "company" ? t("roleCompany") :
        t("roleAdmin");

      toggleLocationField(role);
      toggleExtraField(role);
    });
  });

  toggleLocationField(roleInput.value || "user");
  toggleExtraField(roleInput.value || "user");

  form.addEventListener("submit", (event)=>{
    event.preventDefault();

    const role = roleInput.value || "user";
    const email = document.getElementById("authEmail").value.trim().toLowerCase();
    const password = document.getElementById("authPassword").value.trim();
    const name = document.getElementById("authName").value.trim();
    const phone = phoneInput()?.value.trim() || "";
    const location = document.getElementById("authLocation")?.value.trim() || "";
    const profileNote = extraInput()?.value.trim() || "";

    if(!email || !password){
      alert(getLang()==="ar"
        ? "\u0627\u0643\u062A\u0628\u064A \u0627\u0644\u0625\u064A\u0645\u064A\u0644 \u0648\u0627\u0644\u0628\u0627\u0633\u0648\u0631\u062F"
        : "Enter email and password");
      return;
    }

    if(isSignupMode && (!name || !phone || !location || !profileNote)){
      alert(getLang()==="ar"
        ? "\u0627\u0645\u0644\u0626\u064A \u0643\u0644 \u0628\u064A\u0627\u0646\u0627\u062A \u0627\u0644\u062D\u0633\u0627\u0628 \u0627\u0644\u0623\u0633\u0627\u0633\u064A\u0629"
        : "Please fill all of the basic account details.");
      return;
    }

    localStorage.setItem("sjb_role", role);
    localStorage.setItem("sjb_user", JSON.stringify({
      email,
      name,
      phone,
      location,
      role,
      profileNote
    }));

    const redirect = localStorage.getItem("sjb_redirect");
    if(redirect){
      localStorage.removeItem("sjb_redirect");
      window.location.href = redirect;
      return;
    }

    go(
      role === "user" ? "dashboard-user.html" :
      role === "company" ? "dashboard-company.html" :
      "dashboard-admin.html"
    );
  });
}

function renderUserCVPanel(){
  const panel = document.getElementById("userCvPanel");
  if(!panel) return;

  const cv = getSavedCVBuilder();
  if(!hasSavedCVBuilder()){
    panel.innerHTML = `
      <div class="dashboardCardHead">
        <div>
          <div class="badgeRole">${t("cvNoCvYet")}</div>
          <h3 style="margin:10px 0 0">${t("cvBuilderTitle")}</h3>
          <div class="small" style="margin-top:8px">${t("cvNoCvDesc")}</div>
        </div>
        <a class="btn primary" href="cv-builder.html">${t("openCvBuilder")}</a>
      </div>
    `;
    return;
  }

  const skillList = String(cv.skills || "")
    .split(/[\n,]/)
    .map(item => item.trim())
    .filter(Boolean)
    .slice(0, 5);

  const contactList = [cv.email, cv.phone, cv.location].filter(hasText);
  const subtitle = hasText(cv.role) ? cv.role : (hasText(cv.summary) ? cv.summary : t("cvBuilderDesc"));

  panel.innerHTML = `
    <div class="dashboardCardHead">
      <div>
        <div class="badgeRole">${t("cvSaved")}</div>
        <h3 style="margin:10px 0 0">${escapeHtml(cv.fullName || t("cvBuilderTitle"))}</h3>
        <div class="small" style="margin-top:8px">${escapeHtml(subtitle)}</div>
      </div>
      <a class="btn primary" href="cv-builder.html">${t("openCvBuilder")}</a>
    </div>
    ${contactList.length ? `<div class="meta" style="margin-top:14px">${contactList.map(item => `<span class="pill">${escapeHtml(item)}</span>`).join("")}</div>` : ""}
    ${skillList.length ? `<div class="meta" style="margin-top:8px">${skillList.map(skill => `<span class="pill green">${escapeHtml(skill)}</span>`).join("")}</div>` : ""}
    <div class="small" style="margin-top:14px">${t("cvLastUpdated")}: ${formatSavedCVDate(cv.updatedAt)}</div>
  `;
}

function renderUserDashboard(){
  renderUserCVPanel();

  const wrap = document.getElementById("userAppsTable");
  if(!wrap) return;

  const applications = JSON.parse(localStorage.getItem("sjb_apps") || "[]");
  const jobs = window.SJB_JOBS || [];
  const lang = getLang();

  if(!applications.length){
    wrap.innerHTML = `
      <div class="panel center" style="text-align:center">
        <b>${lang==="ar" ? "\u0644\u0627 \u062A\u0648\u062C\u062F \u062A\u0642\u062F\u064A\u0645\u0627\u062A \u0628\u0639\u062F" : "No applications yet"}</b>
        <div class="small">${lang==="ar" ? "\u0627\u0628\u062F\u0623\u064A \u0648\u0642\u062F\u0645\u064A \u0639\u0644\u0649 \u0648\u0638\u064A\u0641\u0629" : "Start and apply to a job"}</div>
        <div style="margin-top:10px">
          <button class="btn accent" onclick="go('jobs.html')">${lang==="ar" ? "\u062A\u0635\u0641\u062D \u0627\u0644\u0648\u0638\u0627\u0626\u0641" : "Browse Jobs"}</button>
        </div>
      </div>
    `;
    return;
  }

  const rows = applications.map(app => {
    const job = jobs.find(item => item.id === app.jobId);
    const title = job ? (job.title?.[lang] || job.title?.en || "Job") : (lang==="ar" ? "\u0648\u0638\u064A\u0641\u0629" : "Job");

    return `
      <tr>
        <td>${escapeHtml(title)}</td>
        <td>${escapeHtml(app.email)}</td>
        <td>${escapeHtml(app.phone)}</td>
        <td>${escapeHtml(applicationCVText(app))}</td>
      </tr>
    `;
  }).join("");

  wrap.innerHTML = `
    <div class="tableWrap">
      <table class="table">
        <thead>
          <tr>
            <th>${lang==="ar" ? "\u0627\u0644\u0648\u0638\u064A\u0641\u0629" : "Job"}</th>
            <th>${lang==="ar" ? "\u0627\u0644\u0625\u064A\u0645\u064A\u0644" : "Email"}</th>
            <th>${lang==="ar" ? "\u0627\u0644\u0645\u0648\u0628\u0627\u064A\u0644" : "Mobile"}</th>
            <th>CV</th>
          </tr>
        </thead>
        <tbody>${rows}</tbody>
      </table>
    </div>
  `;
}

function openPostJob(){
  const overlay = document.getElementById("postOverlay");
  if(overlay) overlay.style.display = "flex";
}

function closePostJob(){
  const overlay = document.getElementById("postOverlay");
  if(overlay) overlay.style.display = "none";
}

function renderCompanyBillingPanel(){
  const panel = document.getElementById("companyBillingPanel");
  if(!panel) return;

  if(getRole() !== "company"){
    panel.innerHTML = "";
    return;
  }

  const billing = getCurrentCompanyBilling();
  const plan = getCompanyPlanById(billing?.planId);

  if(!billing || !plan){
    panel.innerHTML = `
      <section class="panel companyBillingBanner">
        <div class="dashboardCardHead">
          <div>
            <div class="badgeRole">${t("companyBillingNoPlan")}</div>
            <h3 style="margin:10px 0 0">${t("companyBillingTitle")}</h3>
            <div class="small" style="margin-top:8px">${t("companyBillingNoPlanDesc")}</div>
          </div>
          <a class="btn primary" href="company-payment.html">${t("companyBillingOpen")}</a>
        </div>
      </section>
    `;
    return;
  }

  panel.innerHTML = `
    <section class="panel companyBillingBanner">
      <div class="dashboardCardHead">
        <div>
          <div class="badgeRole">${t("companyBillingActive")}</div>
          <h3 style="margin:10px 0 0">${escapeHtml(plan.name)} - $${plan.price}${t("companyPaymentPerMonth")}</h3>
          <div class="small" style="margin-top:8px">${escapeHtml(plan.description)}</div>
        </div>
        <a class="btn primary" href="company-payment.html">${t("companyBillingManage")}</a>
      </div>
      <div class="meta" style="margin-top:14px">
        <span class="pill">${t("companyBillingCurrentPlan")}: ${escapeHtml(plan.name)}</span>
        <span class="pill green">${t("companyBillingStatus")}: ${t("companyBillingActive")}</span>
        <span class="pill">${t("companyBillingMethod")}: •••• ${escapeHtml(billing.cardLast4 || "0000")}</span>
      </div>
      <div class="meta" style="margin-top:8px">
        <span class="pill">${t("companyBillingLastPaid")}: ${formatBillingDate(billing.lastPaidAt)}</span>
        <span class="pill">${t("companyBillingNextCharge")}: ${formatBillingDate(billing.nextBillingDate)}</span>
      </div>
    </section>
  `;
}

function submitPostJob(event){
  event.preventDefault();

  if(getRole() !== "company"){
    alert(getLang()==="ar"
      ? "\u0647\u0630\u0647 \u0627\u0644\u0635\u0641\u062D\u0629 \u0644\u0644\u0634\u0631\u0643\u0627\u062A \u0641\u0642\u0637"
      : "Company only");
    return;
  }

  const job = {
    id: Date.now(),
    title: document.getElementById("pjTitle").value.trim(),
    location: document.getElementById("pjLocation").value.trim(),
    type: document.getElementById("pjType").value,
    description: document.getElementById("pjDesc").value.trim()
  };

  if(!job.title || !job.location || !job.description){
    alert(getLang()==="ar"
      ? "\u0645\u0646 \u0641\u0636\u0644\u0643 \u0627\u0643\u0645\u0644\u064A \u0627\u0644\u0628\u064A\u0627\u0646\u0627\u062A"
      : "Please fill all fields");
    return;
  }

  const jobs = JSON.parse(localStorage.getItem("sjb_company_jobs") || "[]");
  jobs.push(job);
  localStorage.setItem("sjb_company_jobs", JSON.stringify(jobs));

  alert(getLang()==="ar"
    ? "\u062A\u0645 \u0646\u0634\u0631 \u0627\u0644\u0648\u0638\u064A\u0641\u0629"
    : "Job posted successfully");
  closePostJob();
  renderCompanyDashboard();
}

function renderCompanyDashboard(){
  renderCompanyBillingPanel();

  const wrap = document.getElementById("companyJobsWrap");
  if(!wrap) return;

  const jobs = JSON.parse(localStorage.getItem("sjb_company_jobs") || "[]");
  const user = getUser();
  const lang = getLang();

  if(!jobs.length){
    wrap.innerHTML = `
      <div class="panel center" style="text-align:center">
        <b>${lang==="ar" ? "\u0644\u0645 \u062A\u0642\u0648\u0645\u064A \u0628\u0625\u0636\u0627\u0641\u0629 \u0648\u0638\u0627\u0626\u0641 \u0628\u0639\u062F" : "No jobs posted yet"}</b>
        <div class="small">${lang==="ar" ? "\u0627\u0628\u062F\u0623\u064A \u0628\u0625\u0636\u0627\u0641\u0629 \u0648\u0638\u064A\u0641\u0629 \u062C\u062F\u064A\u062F\u0629" : "Start by posting a new job"}</div>
        <div style="margin-top:10px">
          <button class="btn accent" onclick="openPostJob()">${lang==="ar" ? "\u0625\u0636\u0627\u0641\u0629 \u0648\u0638\u064A\u0641\u0629" : "Post a Job"}</button>
        </div>
      </div>
    `;
    return;
  }

  wrap.innerHTML = `
    <div class="grid">
      ${jobs.map(job => `
        <div class="card">
          <div class="logoBadge"><span>${escapeHtml((user?.name || "C").slice(0, 1))}</span></div>
          <div>
            <h3>${escapeHtml(job.title)}</h3>
            <div class="company">${escapeHtml(user?.name || (lang==="ar" ? "\u0634\u0631\u0643\u0629" : "Company"))}</div>
            <div class="meta">
              <span class="pill green">${escapeHtml(job.location)}</span>
              <span class="pill">${escapeHtml(typeText(job.type))}</span>
            </div>
          </div>
          <div class="right">
            <button class="btn outline" onclick="viewApplicants()">${lang==="ar" ? "\u0639\u0631\u0636 \u0627\u0644\u0645\u062A\u0642\u062F\u0645\u064A\u0646" : "View Applicants"}</button>
          </div>
        </div>
      `).join("")}
    </div>
  `;
}

function viewApplicants(){
  const overlay = document.getElementById("applicantsOverlay");
  const body = document.getElementById("applicantsBody");
  if(!overlay || !body) return;

  overlay.style.display = "flex";

  const applications = JSON.parse(localStorage.getItem("sjb_apps") || "[]");
  const lang = getLang();

  if(!applications.length){
    body.innerHTML = `<div class="small">${lang==="ar" ? "\u0644\u0627 \u064A\u0648\u062C\u062F \u0645\u062A\u0642\u062F\u0645\u064A\u0646 \u0628\u0639\u062F" : "No applicants yet"}</div>`;
    return;
  }

  body.innerHTML = `
    <div class="tableWrap">
      <table class="table">
        <thead>
          <tr>
            <th>${lang==="ar" ? "\u0627\u0644\u0627\u0633\u0645" : "Name"}</th>
            <th>${lang==="ar" ? "\u0627\u0644\u0625\u064A\u0645\u064A\u0644" : "Email"}</th>
            <th>${lang==="ar" ? "\u0627\u0644\u0645\u0648\u0628\u0627\u064A\u0644" : "Mobile"}</th>
            <th>CV</th>
          </tr>
        </thead>
        <tbody>
          ${applications.map(app => `
            <tr>
              <td>${escapeHtml(app.name)}</td>
              <td>${escapeHtml(app.email)}</td>
              <td>${escapeHtml(app.phone)}</td>
              <td>${escapeHtml(applicationCVText(app))}</td>
            </tr>
          `).join("")}
        </tbody>
      </table>
    </div>
  `;
}

function closeApplicants(){
  const overlay = document.getElementById("applicantsOverlay");
  if(overlay) overlay.style.display = "none";
}

function renderAdminDashboard(){
  const wrap = document.getElementById("adminWrap");
  if(!wrap) return;

  if(getRole() !== "admin"){
    wrap.innerHTML = `
      <div class="panel" style="text-align:center">
        <b>${getLang()==="ar" ? "\u063A\u064A\u0631 \u0645\u0635\u0631\u062D" : "Access denied"}</b>
        <div class="small" style="margin-top:8px">${getLang()==="ar" ? "\u0647\u0630\u0647 \u0627\u0644\u0635\u0641\u062D\u0629 \u0644\u0644\u0623\u062F\u0645\u0646 \u0641\u0642\u0637" : "This page is for admin only"}</div>
      </div>
    `;
    return;
  }

  const lang = getLang();
  const applications = JSON.parse(localStorage.getItem("sjb_apps") || "[]");
  const companyJobs = JSON.parse(localStorage.getItem("sjb_company_jobs") || "[]");

  wrap.innerHTML = `
    <div class="panel">
      <div class="stack" style="justify-content:space-between;align-items:center">
        <div>
          <div class="badgeRole">${t("adminManage")}</div>
          <h2 style="margin:10px 0 0">${t("adminSummary")}</h2>
          <div class="small">${lang==="ar" ? "\u0644\u0648\u062D\u0629 \u0628\u0633\u064A\u0637\u0629 \u0644\u0644\u062A\u062E\u0631\u062C (Demo)" : "Simple graduation demo panel"}</div>
        </div>
        <button class="btn outline" onclick="logout()">${t("logout")}</button>
      </div>
      <div class="hr"></div>
      <div class="grid">
        <div class="panel">
          <b>${t("demoJobs")}</b>
          <div style="font-size:34px;font-weight:900;margin-top:8px">${(window.SJB_JOBS || []).length}</div>
        </div>
        <div class="panel">
          <b>${t("companyPostedJobs")}</b>
          <div style="font-size:34px;font-weight:900;margin-top:8px">${companyJobs.length}</div>
        </div>
        <div class="panel">
          <b>${t("applications")}</b>
          <div style="font-size:34px;font-weight:900;margin-top:8px">${applications.length}</div>
        </div>
        <div class="panel">
          <b>${lang==="ar" ? "\u0645\u0644\u0627\u062D\u0638\u0629" : "Note"}</b>
          <div class="small" style="margin-top:8px">
            ${lang==="ar"
              ? "\u062F\u064A \u0646\u0633\u062E\u0629 Demo. \u0639\u0634\u0627\u0646 OAuth \u062D\u0642\u064A\u0642\u064A \u0648\u0642\u0627\u0639\u062F\u0629 \u0628\u064A\u0627\u0646\u0627\u062A \u0644\u0627\u0632\u0645 Backend."
              : "This is a demo. Real OAuth and database support need a backend."}
          </div>
        </div>
      </div>
    </div>
  `;
}

function bindOAuthButtons(){
  const form = document.getElementById("authForm");
  const buttons = document.querySelectorAll(".oauthBtn");
  if(!form || !buttons.length) return;

  buttons.forEach(button => {
    button.addEventListener("click", ()=>{
      const provider = button.dataset.provider || "social";
      const timestamp = Date.now();

      const emailInput = document.getElementById("authEmail");
      const passwordInput = document.getElementById("authPassword");
      const nameInput = document.getElementById("authName");

      if(emailInput) emailInput.value = `${provider}${timestamp}@sjb.demo`;
      if(passwordInput) passwordInput.value = "123456";
      if(nameInput && !nameInput.value.trim()) nameInput.value = `${provider.toUpperCase()} User`;

      if(form.requestSubmit) form.requestSubmit();
      else form.dispatchEvent(new Event("submit", { bubbles: true, cancelable: true }));
    });
  });
}

function getAllCompanyJobs(){
  try { return JSON.parse(localStorage.getItem("sjb_company_jobs") || "[]"); }
  catch { return []; }
}

function saveAllCompanyJobs(jobs){
  localStorage.setItem("sjb_company_jobs", JSON.stringify(jobs));
}

function getCurrentCompanyJobs(){
  const key = getCurrentCompanyBillingKey();
  return getAllCompanyJobs().filter(job => !key || !job.ownerKey || job.ownerKey === key);
}

function getCompanyPlanCatalog(){
  return [
    {
      id: "free",
      price: 0,
      maxPosts: 3,
      name: t("companyPlanFree"),
      description: t("companyPlanFreeDesc"),
      features: [
        t("companyPlanFeaturePosts3"),
        t("companyPlanFeatureApplicants"),
        t("companyPlanFeatureBranding")
      ]
    },
    {
      id: "growth",
      price: 1499,
      maxPosts: 10,
      name: t("companyPlanGrowth"),
      description: t("companyPlanGrowthDesc"),
      features: [
        t("companyPlanFeaturePosts10"),
        t("companyPlanFeatureFeatured"),
        t("companyPlanFeatureSupport")
      ]
    },
    {
      id: "scale",
      price: 3499,
      maxPosts: Number.POSITIVE_INFINITY,
      name: t("companyPlanScale"),
      description: t("companyPlanScaleDesc"),
      features: [
        t("companyPlanFeaturePostsUnlimited"),
        t("companyPlanFeatureAnalytics"),
        t("companyPlanFeatureSupport")
      ]
    }
  ];
}

function getCompanyPlanById(planId){
  return getCompanyPlanCatalog().find(plan => plan.id === planId) || null;
}

function getCurrentCompanyPlan(){
  return getCompanyPlanById(getCurrentCompanyBilling()?.planId) || getCompanyPlanById("free") || getCompanyPlanCatalog()[0];
}

function formatPlanPrice(value){
  const amount = Number(value) || 0;
  if(amount === 0) return t("companyPaymentFree");

  const locale = getLang()==="ar" ? "ar-EG" : "en-US";
  const formatted = amount.toLocaleString(locale);
  return getLang()==="ar" ? `${formatted} ج.م` : `EGP ${formatted}`;
}

function renderCompanyBillingPanel(){
  const panel = document.getElementById("companyBillingPanel");
  if(!panel) return;

  if(getRole() !== "company"){
    panel.innerHTML = "";
    return;
  }

  const billing = getCurrentCompanyBilling();
  const plan = getCurrentCompanyPlan();
  const companyJobs = getCurrentCompanyJobs();
  const planPriceText = plan.price > 0
    ? `${formatPlanPrice(plan.price)} ${t("companyPaymentPerMonth")}`
    : formatPlanPrice(plan.price);
  const postsText = Number.isFinite(plan.maxPosts)
    ? `${companyJobs.length} / ${plan.maxPosts}`
    : t("companyPlanFeaturePostsUnlimited");

  panel.innerHTML = `
    <section class="panel companyBillingBanner">
      <div class="dashboardCardHead">
        <div>
          <div class="badgeRole">${t("companyBillingActive")}</div>
          <h3 style="margin:10px 0 0">${escapeHtml(plan.name)} - ${escapeHtml(planPriceText)}</h3>
          <div class="small" style="margin-top:8px">${escapeHtml(plan.description)}</div>
        </div>
        <a class="btn primary" href="company-payment.html">${t("companyBillingManage")}</a>
      </div>
      <div class="meta" style="margin-top:14px">
        <span class="pill">${t("companyBillingCurrentPlan")}: ${escapeHtml(plan.name)}</span>
        <span class="pill green">${t("companyBillingStatus")}: ${t("companyBillingActive")}</span>
        <span class="pill">${t("companyBillingPostsUsed")}: ${escapeHtml(postsText)}</span>
      </div>
      <div class="meta" style="margin-top:8px">
        <span class="pill">${t("companyBillingMethod")}: ${billing?.cardLast4 ? `•••• ${escapeHtml(billing.cardLast4)}` : "-"}</span>
        <span class="pill">${t("companyBillingLastPaid")}: ${formatBillingDate(billing?.lastPaidAt)}</span>
        <span class="pill">${t("companyBillingNextCharge")}: ${plan.price > 0 ? formatBillingDate(billing?.nextBillingDate) : "-"}</span>
      </div>
    </section>
  `;
}

function submitPostJob(event){
  event.preventDefault();

  if(getRole() !== "company"){
    alert(getLang()==="ar"
      ? "\u0647\u0630\u0647 \u0627\u0644\u0635\u0641\u062D\u0629 \u0644\u0644\u0634\u0631\u0643\u0627\u062A \u0641\u0642\u0637"
      : "Company only");
    return;
  }

  const job = {
    id: Date.now(),
    title: document.getElementById("pjTitle").value.trim(),
    location: document.getElementById("pjLocation").value.trim(),
    type: document.getElementById("pjType").value,
    description: document.getElementById("pjDesc").value.trim(),
    ownerKey: getCurrentCompanyBillingKey(),
    companyName: getUser()?.name || ""
  };

  if(!job.title || !job.location || !job.description){
    alert(getLang()==="ar"
      ? "\u0645\u0646 \u0641\u0636\u0644\u0643 \u0627\u0643\u0645\u0644\u064A \u0627\u0644\u0628\u064A\u0627\u0646\u0627\u062A"
      : "Please fill all fields");
    return;
  }

  const plan = getCurrentCompanyPlan();
  const currentCompanyJobs = getCurrentCompanyJobs();
  if(Number.isFinite(plan.maxPosts) && currentCompanyJobs.length >= plan.maxPosts){
    alert(t("companyPostLimitReached"));
    return;
  }

  const jobs = getAllCompanyJobs();
  jobs.push(job);
  saveAllCompanyJobs(jobs);

  alert(getLang()==="ar"
    ? "\u062A\u0645 \u0646\u0634\u0631 \u0627\u0644\u0648\u0638\u064A\u0641\u0629"
    : "Job posted successfully");
  closePostJob();
  renderCompanyDashboard();
}

function renderCompanyDashboard(){
  renderCompanyBillingPanel();

  const wrap = document.getElementById("companyJobsWrap");
  if(!wrap) return;

  const jobs = getCurrentCompanyJobs();
  const user = getUser();
  const lang = getLang();
  const plan = getCurrentCompanyPlan();

  if(!jobs.length){
    wrap.innerHTML = `
      <div class="panel center" style="text-align:center">
        <b>${lang==="ar" ? "\u0644\u0645 \u062A\u0642\u0648\u0645\u064A \u0628\u0625\u0636\u0627\u0641\u0629 \u0648\u0638\u0627\u0626\u0641 \u0628\u0639\u062F" : "No jobs posted yet"}</b>
        <div class="small">${lang==="ar" ? "\u0627\u0628\u062F\u0623\u064A \u0628\u0625\u0636\u0627\u0641\u0629 \u0648\u0638\u064A\u0641\u0629 \u062C\u062F\u064A\u062F\u0629" : "Start by posting a new job"}</div>
        <div style="margin-top:10px">
          <button class="btn accent" onclick="openPostJob()">${lang==="ar" ? "\u0625\u0636\u0627\u0641\u0629 \u0648\u0638\u064A\u0641\u0629" : "Post a Job"}</button>
        </div>
        <div class="small" style="margin-top:10px">
          ${escapeHtml(t("companyBillingCurrentPlan"))}: ${escapeHtml(plan.name)}
        </div>
      </div>
    `;
    return;
  }

  wrap.innerHTML = `
    <div class="grid">
      ${jobs.map(job => `
        <div class="card">
          <div class="logoBadge"><span>${escapeHtml((user?.name || "C").slice(0, 1))}</span></div>
          <div>
            <h3>${escapeHtml(job.title)}</h3>
            <div class="company">${escapeHtml(job.companyName || user?.name || (lang==="ar" ? "\u0634\u0631\u0643\u0629" : "Company"))}</div>
            <div class="meta">
              <span class="pill green">${escapeHtml(job.location)}</span>
              <span class="pill">${escapeHtml(typeText(job.type))}</span>
            </div>
          </div>
          <div class="right">
            <button class="btn outline" onclick="viewApplicants()">${lang==="ar" ? "\u0639\u0631\u0636 \u0627\u0644\u0645\u062A\u0642\u062F\u0645\u064A\u0646" : "View Applicants"}</button>
          </div>
        </div>
      `).join("")}
    </div>
  `;
}

function getDashboardPathForRole(role){
  return role === "user"
    ? "dashboard-user.html"
    : role === "company"
      ? "dashboard-company.html"
      : "dashboard-admin.html";
}

function getProfilePathForRole(role){
  return role === "user"
    ? "profile-user.html"
    : role === "company"
      ? "profile-company.html"
      : "profile-admin.html";
}

function getCompanyJobStatus(job){
  return String(job?.status || "pending").toLowerCase();
}

function getCurrentCompanyActiveJobs(){
  return getCurrentCompanyJobs().filter(job => getCompanyJobStatus(job) !== "rejected");
}

function getCompanyJobStatusText(status, lang = getLang()){
  if(status === "approved"){
    return lang === "ar" ? "معتمدة" : "Approved";
  }
  if(status === "rejected"){
    return lang === "ar" ? "مرفوضة" : "Rejected";
  }
  return lang === "ar" ? "قيد المراجعة" : "Pending Review";
}

function getCompanyJobStatusClass(status){
  if(status === "approved") return "green";
  if(status === "rejected") return "danger";
  return "warn";
}

function normalizeCompanyJob(job){
  const title = String(job?.title || "").trim();
  const companyName = String(job?.companyName || job?.company || "").trim() || "Company";
  const description = String(job?.description || "").trim();

  return {
    id: job.id,
    title: { en: title, ar: title },
    company: { en: companyName, ar: companyName },
    location: job.location || "",
    type: job.type || "",
    salary: job.salary || "",
    description: { en: description, ar: description },
    requirements: { en: [], ar: [] },
    source: "company",
    status: getCompanyJobStatus(job),
    ownerKey: job.ownerKey || "",
    submittedAt: job.submittedAt || job.createdAt || "",
    reviewedAt: job.reviewedAt || ""
  };
}

function getApprovedCompanyJobs(){
  return getAllCompanyJobs()
    .filter(job => getCompanyJobStatus(job) === "approved")
    .map(normalizeCompanyJob);
}

function getAllLiveJobs(){
  return [...(window.SJB_JOBS || []), ...getApprovedCompanyJobs()];
}

function getJobById(jobId){
  return getAllLiveJobs().find(job => String(job.id) === String(jobId)) || null;
}

function updateCompanyJobStatus(jobId, status){
  const jobs = getAllCompanyJobs();
  const job = jobs.find(item => String(item.id) === String(jobId));
  if(!job) return false;

  job.status = status;
  job.reviewedAt = new Date().toISOString();
  saveAllCompanyJobs(jobs);
  return true;
}

window.reviewCompanyJob = function(jobId, status){
  if(!updateCompanyJobStatus(jobId, status)) return;

  alert(
    getLang()==="ar"
      ? (status === "approved" ? "تم اعتماد الوظيفة" : "تم رفض الوظيفة")
      : (status === "approved" ? "Job approved" : "Job rejected")
  );

  renderAdminDashboard();
  renderCompanyDashboard();
  renderJobs();
  renderJobDetails();
};

function renderJobs(){
  const grid = document.querySelector("main #jobsGrid") || document.getElementById("jobsGrid");
  if(!grid) return;

  const lang = getLang();
  const loc = document.getElementById("locSelect")?.value || "all";
  const type = document.getElementById("typeSelect")?.value || "all";
  const resultCount = document.getElementById("resultCount");

  const jobs = getAllLiveJobs().filter(job => {
    const locationOk = (loc === "all") || (norm(job.location) === norm(loc));
    const typeOk = (type === "all") || (norm(job.type).replace(/\s/g, "") === norm(type));
    return locationOk && typeOk;
  });

  if(resultCount){
    resultCount.textContent = lang === "ar"
      ? `${jobs.length} وظيفة متاحة`
      : `${jobs.length} jobs available`;
  }

  if(!jobs.length){
    grid.innerHTML = `
      <div class="panel" style="grid-column:1/-1;text-align:center">
        <b>${lang === "ar" ? "لا توجد وظائف" : "No jobs found"}</b>
        <div class="small" style="margin-top:8px">
          ${lang === "ar" ? "وظائف الشركات تظهر هنا بعد اعتمادها من الأدمن." : "Company jobs appear here after admin approval."}
        </div>
      </div>
    `;
    return;
  }

  grid.innerHTML = jobs.map(job => {
    const title = job.title?.[lang] || job.title?.en || "";
    const company = job.company?.[lang] || job.company?.en || "";

    return `
      <div class="card">
        <div class="logoBadge"><span>${escapeHtml(company.trim().slice(0, 1) || "J")}</span></div>
        <div>
          <h3>${escapeHtml(title)}</h3>
          <div class="company">${escapeHtml(company)}</div>
          <div class="meta">
            <span class="pill green">${escapeHtml(locationText(job.location))}</span>
            <span class="pill">${escapeHtml(typeText(job.type))}</span>
          </div>
        </div>
        <div class="right">
          <div class="salary">${escapeHtml(job.salary || "")}</div>
          <button class="btn accent" onclick="go('job.html?id=${job.id}')">${t("viewDetails")}</button>
        </div>
      </div>
    `;
  }).join("");
}

function renderJobDetails(){
  const wrap = document.getElementById("jobDetailsWrap");
  if(!wrap) return;

  const id = qs("id");
  const job = getJobById(id);

  if(!id || !job){
    wrap.innerHTML = `
      <div class="panel" style="text-align:center">
        <b>${t("missingIdTitle")}</b>
        <div class="small" style="margin-top:8px">${t("missingIdDesc")}</div>
        <div style="margin-top:12px">
          <button class="btn accent" onclick="go('jobs.html')">${t("backJobs")}</button>
        </div>
      </div>
    `;
    return;
  }

  const lang = getLang();
  const title = job.title?.[lang] || job.title?.en || "";
  const company = job.company?.[lang] || job.company?.en || "";
  const description = job.description?.[lang] || job.description?.en || "";
  const requirements = Array.isArray(job.requirements?.[lang])
    ? job.requirements[lang]
    : (Array.isArray(job.requirements?.en) ? job.requirements.en : []);

  wrap.innerHTML = `
    <div class="panel">
      <div style="display:flex;justify-content:space-between;gap:12px;flex-wrap:wrap;align-items:center">
        <div>
          <div class="badgeRole">${escapeHtml(company)}</div>
          <h2 style="margin:10px 0 0">${escapeHtml(title)}</h2>
          <div class="meta" style="margin-top:10px">
            <span class="pill green">${escapeHtml(locationText(job.location))}</span>
            <span class="pill">${escapeHtml(typeText(job.type))}</span>
            ${job.salary ? `<span class="pill">${escapeHtml(job.salary)}</span>` : ""}
          </div>
        </div>
        <div class="stack">
          <button class="btn outline" onclick="go('jobs.html')">${t("backJobs")}</button>
          <button class="btn accent" onclick="openApply(${job.id})">${t("applyNow")}</button>
        </div>
      </div>
      <div class="hr"></div>
      <h3>${t("description")}</h3>
      <p style="line-height:1.8;margin-top:8px;font-weight:700;color:#334155">${escapeHtml(description)}</p>
      <h3 style="margin-top:16px">${t("requirements")}</h3>
      ${requirements.length
        ? `<ul style="margin-top:8px;font-weight:700;color:#334155;line-height:1.9">${requirements.map(item => `<li>${escapeHtml(item)}</li>`).join("")}</ul>`
        : `<div class="small" style="margin-top:8px">${lang === "ar" ? "لم تتم إضافة متطلبات بعد." : "No extra requirements were added."}</div>`}
    </div>
  `;
}

function renderUserDashboard(){
  renderUserCVPanel();

  const wrap = document.getElementById("userAppsTable");
  if(!wrap) return;

  const applications = JSON.parse(localStorage.getItem("sjb_apps") || "[]");
  const jobs = getAllLiveJobs();
  const lang = getLang();
  const currentUser = getUser();
  const filteredApps = currentUser
    ? applications.filter(app => String(app.email || "").toLowerCase() === String(currentUser.email || "").toLowerCase())
    : applications;

  if(!filteredApps.length){
    wrap.innerHTML = `
      <div class="panel center" style="text-align:center">
        <b>${lang==="ar" ? "لا توجد تقديمات بعد" : "No applications yet"}</b>
        <div class="small">${lang==="ar" ? "ابدأي وقدمي على وظيفة" : "Start and apply to a job"}</div>
        <div style="margin-top:10px">
          <button class="btn accent" onclick="go('jobs.html')">${lang==="ar" ? "تصفح الوظائف" : "Browse Jobs"}</button>
        </div>
      </div>
    `;
    return;
  }

  const rows = filteredApps.map(app => {
    const job = jobs.find(item => String(item.id) === String(app.jobId));
    const title = job ? (job.title?.[lang] || job.title?.en || "Job") : (lang==="ar" ? "وظيفة" : "Job");

    return `
      <tr>
        <td>${escapeHtml(title)}</td>
        <td>${escapeHtml(app.email)}</td>
        <td>${escapeHtml(app.phone)}</td>
        <td>${escapeHtml(applicationCVText(app))}</td>
      </tr>
    `;
  }).join("");

  wrap.innerHTML = `
    <div class="tableWrap">
      <table class="table">
        <thead>
          <tr>
            <th>${lang==="ar" ? "الوظيفة" : "Job"}</th>
            <th>${lang==="ar" ? "الإيميل" : "Email"}</th>
            <th>${lang==="ar" ? "الموبايل" : "Mobile"}</th>
            <th>CV</th>
          </tr>
        </thead>
        <tbody>${rows}</tbody>
      </table>
    </div>
  `;
}

function renderCompanyBillingPanel(){
  const panel = document.getElementById("companyBillingPanel");
  if(!panel) return;

  if(getRole() !== "company"){
    panel.innerHTML = "";
    return;
  }

  const billing = getCurrentCompanyBilling();
  const plan = getCurrentCompanyPlan();
  const companyJobs = getCurrentCompanyActiveJobs();
  const planPriceText = plan.price > 0
    ? `${formatPlanPrice(plan.price)} ${t("companyPaymentPerMonth")}`
    : formatPlanPrice(plan.price);
  const postsText = Number.isFinite(plan.maxPosts)
    ? `${companyJobs.length} / ${plan.maxPosts}`
    : t("companyPlanFeaturePostsUnlimited");

  panel.innerHTML = `
    <section class="panel companyBillingBanner">
      <div class="dashboardCardHead">
        <div>
          <div class="badgeRole">${t("companyBillingActive")}</div>
          <h3 style="margin:10px 0 0">${escapeHtml(plan.name)} - ${escapeHtml(planPriceText)}</h3>
          <div class="small" style="margin-top:8px">${escapeHtml(plan.description)}</div>
        </div>
        <a class="btn primary" href="company-payment.html">${t("companyBillingManage")}</a>
      </div>
      <div class="meta" style="margin-top:14px">
        <span class="pill">${t("companyBillingCurrentPlan")}: ${escapeHtml(plan.name)}</span>
        <span class="pill green">${t("companyBillingStatus")}: ${t("companyBillingActive")}</span>
        <span class="pill">${t("companyBillingPostsUsed")}: ${escapeHtml(postsText)}</span>
      </div>
      <div class="meta" style="margin-top:8px">
        <span class="pill">${t("companyBillingMethod")}: ${billing?.cardLast4 ? `•••• ${escapeHtml(billing.cardLast4)}` : "-"}</span>
        <span class="pill">${t("companyBillingLastPaid")}: ${formatBillingDate(billing?.lastPaidAt)}</span>
        <span class="pill">${t("companyBillingNextCharge")}: ${plan.price > 0 ? formatBillingDate(billing?.nextBillingDate) : "-"}</span>
      </div>
    </section>
  `;
}

function submitPostJob(event){
  event.preventDefault();

  if(getRole() !== "company"){
    alert(getLang()==="ar"
      ? "هذه الصفحة للشركات فقط"
      : "Company only");
    return;
  }

  const job = {
    id: Date.now(),
    title: document.getElementById("pjTitle").value.trim(),
    location: document.getElementById("pjLocation").value.trim(),
    type: document.getElementById("pjType").value,
    description: document.getElementById("pjDesc").value.trim(),
    ownerKey: getCurrentCompanyBillingKey(),
    companyName: getUser()?.name || "",
    status: "pending",
    submittedAt: new Date().toISOString()
  };

  if(!job.title || !job.location || !job.description){
    alert(getLang()==="ar"
      ? "من فضلك اكملي البيانات"
      : "Please fill all fields");
    return;
  }

  const plan = getCurrentCompanyPlan();
  const currentCompanyJobs = getCurrentCompanyActiveJobs();
  if(Number.isFinite(plan.maxPosts) && currentCompanyJobs.length >= plan.maxPosts){
    alert(t("companyPostLimitReached"));
    return;
  }

  const jobs = getAllCompanyJobs();
  jobs.push(job);
  saveAllCompanyJobs(jobs);

  alert(getLang()==="ar"
    ? "تم إرسال الوظيفة للأدمن للمراجعة"
    : "Job submitted for admin review");
  closePostJob();
  renderCompanyDashboard();
}

function renderCompanyDashboard(){
  renderCompanyBillingPanel();

  const wrap = document.getElementById("companyJobsWrap");
  if(!wrap) return;

  const jobs = getCurrentCompanyJobs();
  const user = getUser();
  const lang = getLang();
  const plan = getCurrentCompanyPlan();

  if(!jobs.length){
    wrap.innerHTML = `
      <div class="panel center" style="text-align:center">
        <b>${lang==="ar" ? "لم تقومي بإضافة وظائف بعد" : "No jobs posted yet"}</b>
        <div class="small">${lang==="ar" ? "ابدأي بإضافة وظيفة جديدة" : "Start by posting a new job"}</div>
        <div style="margin-top:10px">
          <button class="btn accent" onclick="openPostJob()">${lang==="ar" ? "إضافة وظيفة" : "Post a Job"}</button>
        </div>
        <div class="small" style="margin-top:10px">
          ${escapeHtml(t("companyBillingCurrentPlan"))}: ${escapeHtml(plan.name)}
        </div>
      </div>
    `;
    return;
  }

  wrap.innerHTML = `
    <div class="grid">
      ${jobs.map(job => {
        const status = getCompanyJobStatus(job);
        const statusText = getCompanyJobStatusText(status, lang);
        const statusClass = getCompanyJobStatusClass(status);
        const statusHint = status === "approved"
          ? (lang === "ar" ? "هذه الوظيفة ظاهرة الآن للطلاب." : "This job is now visible to students.")
          : status === "rejected"
            ? (lang === "ar" ? "هذه الوظيفة مرفوضة ومخفية من صفحة الوظائف." : "This job was rejected and is hidden from the jobs page.")
            : (lang === "ar" ? "في انتظار مراجعة الأدمن قبل النشر." : "Waiting for admin review before going live.");

        return `
          <div class="card">
            <div class="logoBadge"><span>${escapeHtml((user?.name || "C").slice(0, 1))}</span></div>
            <div>
              <div class="meta" style="margin-bottom:8px">
                <span class="pill ${statusClass === "green" ? "green" : ""}" style="${statusClass === "warn" ? "background:#fef3c7;color:#92400e" : statusClass === "danger" ? "background:#fee2e2;color:#991b1b" : ""}">${escapeHtml(statusText)}</span>
              </div>
              <h3>${escapeHtml(job.title)}</h3>
              <div class="company">${escapeHtml(job.companyName || user?.name || (lang==="ar" ? "شركة" : "Company"))}</div>
              <div class="meta">
                <span class="pill green">${escapeHtml(job.location)}</span>
                <span class="pill">${escapeHtml(typeText(job.type))}</span>
              </div>
              <div class="small" style="margin-top:10px">${escapeHtml(statusHint)}</div>
            </div>
            <div class="right">
              <button class="btn outline" onclick="viewApplicants(${job.id})">${lang==="ar" ? "عرض المتقدمين" : "View Applicants"}</button>
            </div>
          </div>
        `;
      }).join("")}
    </div>
  `;
}

function viewApplicants(jobId){
  const overlay = document.getElementById("applicantsOverlay");
  const body = document.getElementById("applicantsBody");
  if(!overlay || !body) return;

  overlay.style.display = "flex";

  const applications = JSON.parse(localStorage.getItem("sjb_apps") || "[]");
  const lang = getLang();
  const jobIds = jobId
    ? [String(jobId)]
    : getCurrentCompanyJobs().map(job => String(job.id));
  const filteredApps = applications.filter(app => jobIds.includes(String(app.jobId)));

  if(!filteredApps.length){
    body.innerHTML = `<div class="small">${lang==="ar" ? "لا يوجد متقدمين بعد" : "No applicants yet"}</div>`;
    return;
  }

  body.innerHTML = `
    <div class="tableWrap">
      <table class="table">
        <thead>
          <tr>
            <th>${lang==="ar" ? "الاسم" : "Name"}</th>
            <th>${lang==="ar" ? "الإيميل" : "Email"}</th>
            <th>${lang==="ar" ? "الموبايل" : "Mobile"}</th>
            <th>CV</th>
          </tr>
        </thead>
        <tbody>
          ${filteredApps.map(app => `
            <tr>
              <td>${escapeHtml(app.name)}</td>
              <td>${escapeHtml(app.email)}</td>
              <td>${escapeHtml(app.phone)}</td>
              <td>${escapeHtml(applicationCVText(app))}</td>
            </tr>
          `).join("")}
        </tbody>
      </table>
    </div>
  `;
}

function renderAdminDashboard(){
  const wrap = document.getElementById("adminWrap");
  if(!wrap) return;

  if(getRole() !== "admin"){
    wrap.innerHTML = `
      <div class="panel" style="text-align:center">
        <b>${getLang()==="ar" ? "غير مصرح" : "Access denied"}</b>
        <div class="small" style="margin-top:8px">${getLang()==="ar" ? "هذه الصفحة للأدمن فقط" : "This page is for admin only"}</div>
      </div>
    `;
    return;
  }

  const lang = getLang();
  const applications = JSON.parse(localStorage.getItem("sjb_apps") || "[]");
  const companyJobs = getAllCompanyJobs();
  const pendingJobs = companyJobs.filter(job => getCompanyJobStatus(job) === "pending");
  const approvedJobs = companyJobs.filter(job => getCompanyJobStatus(job) === "approved");
  const rejectedJobs = companyJobs.filter(job => getCompanyJobStatus(job) === "rejected");

  const approvalRows = companyJobs.length
    ? companyJobs
        .slice()
        .sort((a, b) => {
          const order = { pending: 0, approved: 1, rejected: 2 };
          return (order[getCompanyJobStatus(a)] ?? 9) - (order[getCompanyJobStatus(b)] ?? 9) || Number(b.id) - Number(a.id);
        })
        .map(job => {
          const status = getCompanyJobStatus(job);
          const statusText = getCompanyJobStatusText(status, lang);
          const badgeStyle = status === "approved"
            ? "background:#dcfce7;color:#166534"
            : status === "rejected"
              ? "background:#fee2e2;color:#991b1b"
              : "background:#fef3c7;color:#92400e";

          return `
            <tr>
              <td>
                <b>${escapeHtml(job.title)}</b>
                <div class="small" style="margin-top:4px">${escapeHtml(job.companyName || (lang==="ar" ? "شركة" : "Company"))}</div>
              </td>
              <td>${escapeHtml(job.location)}</td>
              <td>${escapeHtml(typeText(job.type))}</td>
              <td><span class="pill" style="${badgeStyle}">${escapeHtml(statusText)}</span></td>
              <td>
                <div class="stack" style="gap:8px;justify-content:flex-end">
                  <button class="btn primary" type="button" onclick="reviewCompanyJob(${job.id}, 'approved')">${lang==="ar" ? "اعتماد" : "Approve"}</button>
                  <button class="btn outline" type="button" style="border-color:#ef4444;color:#b91c1c" onclick="reviewCompanyJob(${job.id}, 'rejected')">${lang==="ar" ? "رفض" : "Reject"}</button>
                </div>
              </td>
            </tr>
          `;
        }).join("")
    : `
      <tr>
        <td colspan="5" style="text-align:center">${lang==="ar" ? "لا توجد وظائف مرسلة من الشركات بعد." : "No company jobs have been submitted yet."}</td>
      </tr>
    `;

  wrap.innerHTML = `
    <div class="panel">
      <div class="stack" style="justify-content:space-between;align-items:center">
        <div>
          <div class="badgeRole">${t("adminManage")}</div>
          <h2 style="margin:10px 0 0">${t("adminSummary")}</h2>
          <div class="small">${lang==="ar" ? "راجعي الوظائف القادمة من الشركات قبل ظهورها للطلاب." : "Review company job posts before they appear to students."}</div>
        </div>
      </div>
      <div class="hr"></div>
      <div class="grid">
        <div class="panel">
          <b>${t("demoJobs")}</b>
          <div style="font-size:34px;font-weight:900;margin-top:8px">${(window.SJB_JOBS || []).length}</div>
        </div>
        <div class="panel">
          <b>${t("companyPostedJobs")}</b>
          <div style="font-size:34px;font-weight:900;margin-top:8px">${companyJobs.length}</div>
        </div>
        <div class="panel">
          <b>${lang==="ar" ? "قيد المراجعة" : "Pending Review"}</b>
          <div style="font-size:34px;font-weight:900;margin-top:8px">${pendingJobs.length}</div>
        </div>
        <div class="panel">
          <b>${lang==="ar" ? "المعتمدة" : "Approved"}</b>
          <div style="font-size:34px;font-weight:900;margin-top:8px">${approvedJobs.length}</div>
        </div>
        <div class="panel">
          <b>${lang==="ar" ? "المرفوضة" : "Rejected"}</b>
          <div style="font-size:34px;font-weight:900;margin-top:8px">${rejectedJobs.length}</div>
        </div>
        <div class="panel">
          <b>${t("applications")}</b>
          <div style="font-size:34px;font-weight:900;margin-top:8px">${applications.length}</div>
        </div>
      </div>
    </div>

    <section class="panel" style="margin-top:18px">
      <div class="dashboardCardHead">
        <div>
          <div class="badgeRole">${lang==="ar" ? "مراجعة الوظائف" : "Job Review"}</div>
          <h3 style="margin:10px 0 0">${lang==="ar" ? "اعتماد أو رفض وظائف الشركات" : "Approve or reject company job posts"}</h3>
          <div class="small" style="margin-top:8px">${lang==="ar" ? "الوظائف المعتمدة فقط تظهر في صفحة الوظائف للطلاب." : "Only approved jobs are shown on the jobs page for students."}</div>
        </div>
      </div>
      <div class="tableWrap" style="margin-top:16px">
        <table class="table">
          <thead>
            <tr>
              <th>${lang==="ar" ? "الوظيفة" : "Job"}</th>
              <th>${lang==="ar" ? "المكان" : "Location"}</th>
              <th>${lang==="ar" ? "النوع" : "Type"}</th>
              <th>${lang==="ar" ? "الحالة" : "Status"}</th>
              <th>${lang==="ar" ? "الإجراء" : "Action"}</th>
            </tr>
          </thead>
          <tbody>${approvalRows}</tbody>
        </table>
      </div>
    </section>
  `;
}

function bindOAuthButtons(){
  const buttons = document.querySelectorAll(".oauthBtn");
  if(!buttons.length) return;

  buttons.forEach(button => {
    button.addEventListener("click", (event)=>{
      const provider = button.dataset.provider || "";
      const role = document.getElementById("authRole")?.value || "user";
      const backendRole = role === "user" ? "student" : role;

      if(provider === "google" || provider === "facebook"){
        if(backendRole === "admin"){
          event.preventDefault();
          alert(getLang()==="ar"
            ? "دخول الأدمن يتم بالإيميل وكلمة المرور فقط."
            : "Admin sign-in is available with email and password only.");
          return;
        }

        const returnTo = new URL(getDashboardPathForRole(role), window.location.href).href;
        const authUrl = provider === "google"
          ? `http://127.0.0.1:8000/oauth/google/?role=${encodeURIComponent(backendRole)}&return_to=${encodeURIComponent(returnTo)}`
          : `http://127.0.0.1:8000/oauth/facebook/?role=${encodeURIComponent(backendRole)}&return_to=${encodeURIComponent(returnTo)}`;
        window.location.href = authUrl;
        return;
      }

      event.preventDefault();
      alert(getLang()==="ar"
        ? "هذا المزود غير مفعّل حالياً. استخدم Google أو Facebook أو تسجيل الدخول العادي."
        : "This provider is not configured yet. Please use Google, Facebook, or the regular sign-in form.");
    });
  });
}

document.addEventListener("DOMContentLoaded", ()=>{
  if(typeof applyI18N === "function") applyI18N();

  initLanguageButton();
  initNavActive();

  renderJobs();
  renderJobDetails();
  authInit();
  renderUserDashboard();
  renderCompanyDashboard();
  renderAdminDashboard();

  const applyForm = document.getElementById("applyForm");
  if(applyForm) applyForm.addEventListener("submit", submitApplication);

  const useSavedCV = document.getElementById("useSavedCv");
  if(useSavedCV) useSavedCV.addEventListener("change", syncSavedCVOption);
  syncSavedCVOption();

  const postJobForm = document.getElementById("postJobForm");
  if(postJobForm) postJobForm.addEventListener("submit", submitPostJob);

  bindOAuthButtons();
});
